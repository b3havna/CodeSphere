# CodeSphere Frontend Environment Variables
# Replace with your actual Render backend deployment URL (no trailing slash)
REACT_APP_API_URL=https://your-backend.onrender.com

# Disable ESLint plugin during build to prevent configuration conflicts
DISABLE_ESLINT_PLUGIN=true
